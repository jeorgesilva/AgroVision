# Rename this file to secrets.toml and fill in the actual credentials.
# Never commit secrets.toml to version control — it is already listed in .gitignore.

[auth]
username = "admin"
password = "change_me"
